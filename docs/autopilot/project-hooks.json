{
  "_doc": "Project hooks for Autopilot scripts (ai-transformation-io).",
  "prod_smoke_cmd": "curl -fsS -o /dev/null -w '%{http_code}' https://ai-transformation.io/ | grep -E '^(200|301|302)$' && curl -fsS -o /dev/null -w '%{http_code}' https://ai-transformation.org/ | grep -E '^(200|301|302)$'",
  "notes": "Both public sites must return 2xx/3xx. Override with env PROD_SMOKE_CMD if needed. Zeabur deploy can lag after merge — Checker WATCHDOG should allow a few minutes."
}
